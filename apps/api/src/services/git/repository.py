"""
Git repository service.

Provides git operations within a workspace: status, diff, branch management,
patch generation, and commit drafting. Patches are shown as unified diffs
for human review before application via `git apply`.
"""


class GitRepository:
    # TODO: implement git operations scoped to a workspace path
    pass
